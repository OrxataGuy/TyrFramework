#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Ejecutando tests completos antes del push..."
echo ""

# # Ejecutar suite completa de tests si está disponible
# if grep -q "\"test\":" package.json; then
#   npm test
#   TEST_EXIT_CODE=$?
# else
#   # Si no hay suite completa, al menos ejecutar smoke tests
#   npm run test:smoke
#   TEST_EXIT_CODE=$?
# fi

# Si los tests fallan, abortar el push
if [ $TEST_EXIT_CODE -ne 0 ]; then
  echo ""
  echo "❌ PUSH ABORTADO: Los tests no pasaron"
  echo ""
  echo "💡 Soluciones:"
  echo "   1. Revisa los errores mostrados arriba"
  echo "   2. Arregla los comandos que fallan"
  echo "   3. Ejecuta 'npm test' manualmente para verificar"
  echo "   4. Intenta hacer push de nuevo"
  echo ""
  echo "⚠️  Para saltarte esta validación (NO RECOMENDADO):"
  echo "   git push --no-verify"
  echo ""
  exit 1
fi

echo ""
echo "✅ Tests completos pasados! Continuando con el push..."
echo ""
